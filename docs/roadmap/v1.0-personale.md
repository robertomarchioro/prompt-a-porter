# PaP Personale — Scope v1.0.0

> **SKU**: Prompt a Porter Personale (target single-user, local-first, no server obbligatorio).
> **Deliverable**: tag release `v1.0.0` — uscita beta del prodotto-utente.
> **Versione di partenza**: `v0.8.8` (2026-05-11).
> **Stima orizzonte**: 2-3 mesi se cert Authenticode Certum arriva entro fine mese.

## Filosofia v1.0

> Penelope ha finito la tela. Ulisse è tornato. Nel viaggio dalle Fasi 1 a 4 il prodotto ha acquisito tutte le capability che servono a una persona singola per gestire i propri prompt AI come asset di valore — versioning, varianti, regression testing, ricerca semantica, prompt componibili. Resta da rifinire l'esperienza e mettere in sicurezza la distribuzione. Quando arriva v1.0, PaP smette di essere "in beta" per l'utente individuale.

## Cosa v1.0 promette all'utente

1. **Installazione zero-friction** su Windows: NSIS per-user senza richieste UAC, auto-update silenzioso che applica patch in background, Authenticode signing che elimina SmartScreen warning su prima esecuzione.
2. **Editor curato**: tutte le scorciatoie + UI Editor "Crea variante" inline, doppia vista Sorgente/Compilato integrata (non più modale separato), markdown export con front-matter e import roundtrip.
3. **Prompt componibili evoluti**: sintassi `{{import "x" with k=v}}` (variabili scopate) + `{{import "x" version=N}}` (pinning a versione storica). Editor con hover preview e Ctrl+click navigazione già live in v0.7.0.
4. **Accessibilità di base**: 0 warning a11y in `svelte-check` (oggi ~37).
5. **Stabilità**: coverage Rust client ≥ 80%, TS client ≥ 70%, regression suite verde, security audit pulito.
6. **Documentazione utente completa**: getting started, casi d'uso comuni, troubleshooting, glossario sintassi.

## Cosa v1.0 NON promette (è in v2.0)

- Multi-utente, workspace team
- Sync server obbligatorio (resta opzionale)
- SSO / OIDC / SAML
- E2E encryption
- Web app / Browser extension
- Approval workflow + RBAC cartelle
- MCP HTTP/SSE (resta solo stdio)
- API pubblica server-side

---

## Scope — must-have per v1.0

### M1. Auto-update + Authenticode signing 🚧

**Origine**: Step 5 di Fase 2, patch line `v0.2.x`. Vedi [`rinvii.md`](./rinvii.md) §1 e [`fase-2-foundations.md`](./fase-2-foundations.md) Step 5 deferred.

**Stato (2026-05-15)**: 🚧 **sbloccato**. Cert Certum SimplySign Cloud (API-based, CI-friendly) ricevuto. Lavoro avviabile in qualunque momento, da pianificare in parallelo a M2/M3.

**Approccio tecnico SimplySign Cloud**: il certificato risiede nel cloud Certum, firma via REST API autenticata. Niente HSM USB, niente self-hosted runner necessari. Pipeline GitHub Actions integra con secret `CERTUM_SIMPLYSIGN_USERNAME` + `CERTUM_SIMPLYSIGN_PASSWORD` (o token API), step `signtool.exe` cloud-aware invocato dopo `tauri build`.

**Sotto-deliverable**:
- [ ] NSIS installer per-user (no-UAC, target `%LocalAppData%`).
- [ ] Tauri Updater client configurato con endpoint statico (GitHub Pages o repo `latest.json`).
- [ ] `latest.json` firmato durante release pipeline.
- [ ] Downgrade refuse + signature mismatch refuse.
- [ ] Authenticode signing su tutti gli `.exe` di release.
- [ ] `docs/utente/auto-update.md` con meccanica + troubleshooting + recovery update corrotto.
- [ ] `docs/architettura/decisioni/authenticode-signing.md` con provider considerati + criteri attivazione.
- [ ] Test E2E Tauri Updater: build dev → fake server → download + apply, downgrade refuse, signature mismatch refuse.
- [ ] Smoke test installer NSIS per-user su Win10 + Win11.

**Sequenza sub-PR raccomandata**:
- **M1.1** — ADR `docs/architettura/decisioni/authenticode-signing.md`: provider valutati (Certum SimplySign vs HSM standard vs EV), criteri di attivazione, scelta SimplySign Cloud con razionale CI-friendly.
- **M1.2** — Workflow `release.yml`: step `signtool` cloud SimplySign dopo `tauri build`, secret repo configurati, smoke run su tag di test.
- **M1.3** — `tauri.conf.json` NSIS per-user (no UAC, target `%LocalAppData%`).
- **M1.4** — Tauri Updater client + endpoint statico `latest.json` su GitHub Pages (riusa workflow website se attivo) o repo dedicato.
- **M1.5** — Firma `latest.json` + downgrade refuse + signature mismatch refuse implementati.
- **M1.6** — Test E2E Tauri Updater (build dev → fake server → apply/refuse) + smoke installer Win10/Win11.
- **M1.7** — Doc utente `docs/utente/auto-update.md` con meccanica + troubleshooting + recovery update corrotto.

**Lavoro stimato**: 7 sub-PR atterrabili in serie, ~10 giorni di sviluppo + testing. Parallelo a M2/M3 senza conflitti (touch su layer disgiunti: workflow CI, packaging, doc).

### M2. Pulizia warning a11y svelte-check 🎨

**Origine**: era target storico v0.6.0, slittato perché v0.6.0 ha fatto hardening backend.

**Stato attuale**: ~37 warning `svelte-check`: autofocus, label senza control, role/aria props mismatch, noninteractive con tabindex, etc.

**Sotto-deliverable**:
- [ ] Audit completo warning con script che li classifica per categoria.
- [ ] Fix autofocus (sostituire con focus manuale via `bind:this` + `onMount`).
- [ ] Fix label senza control associato (aggiungere `for=` o wrap input).
- [ ] Fix role/aria props inconsistenti.
- [ ] Fix noninteractive tabindex (rimuovere o cambiare elemento).
- [ ] Gate CI: `svelte-check --fail-on-warnings` su a11y.

**Lavoro stimato**: 3-5 PR atomiche, 2-3 giorni.

### M3. Recupero sub-step UI Fase 4 ✅ (chiusa 2026-05-16)

Quick win frontend che hanno valore percepito alto.

**Sotto-deliverable**:
- [x] **UI Editor "Crea variante"** dentro `DetailPane` (oggi solo da Libreria). Bottone + modale conferma con preselected variant label. → PR #203 (M3 PR-1)
- [x] **Modale "Aggiungi nota"** su voto negativo rating. → PR #204 (M3 PR-2)
- [x] **"Esegui tutti i golden" batch** in vista Test, con riassunto pass/fail. → PR #205 (M3 PR-3)
- [x] **Renderer dropdown variante** con switch al volo mantenendo i valori del form Compila. → PR #206 (M3 PR-4)
- [x] **Promuovi variante a principale** (swap main ↔ variant con preservazione storia). → PR #207 (M3 PR-5). Backlog: migration automatica backreference (`rinvii.md`).
- [x] **Sort by quality "Migliori prompt"** in Libreria → già implementato in F11 ListPane redesign (`ListPane.svelte:220` opzione "Migliori" + backend `libreria.rs:172-180` AVG rating 90 giorni). Nessuna PR M3 necessaria.

**Risultato**: 5 PR atomiche mergiate (#203-207) + 1 sub-deliverable pre-esistente.
**Lavoro effettivo**: ~3 ore vs 3-5 giorni stimati.

### M4. Sintassi import evoluta — Fase 3 sub-step 📋

**Sotto-deliverable**:
- [ ] Parser `{{import "x" with k=v}}` con variabili scopate per import. Schema decodifica già pensato, manca parser e applicazione al compile.
- [ ] Parser `{{import "x" version=N}}` con pinning a versione storica. Schema `PromptVersions` già pronto, manca solo parser + lookup.
- [ ] Linting nuove regole IMP* per validare sintassi (es. variabile dichiarata in `with` ma non usata, version inesistente, etc.).
- [ ] Test compatibility: vecchia sintassi `{{import "x"}}` resta funzionante.
- [ ] Doc utente: `docs/utente/prompt-componibili.md` aggiornato con esempi.

**Lavoro stimato**: 2-3 PR, 4-5 giorni (parser sensibile).

### M5. Editor doppia vista Sorgente/Compilato integrata 📋

**Stato attuale**: esiste come `CompilaModal` separato.

**Sotto-deliverable**:
- [ ] Tab/split-view in `DetailPane` che mostra sorgente a sinistra e preview compilato a destra (con form valori inline).
- [ ] Mantenere `CompilaModal` come fallback per "Compila & copia rapido" da palette globale.
- [ ] Sync bidirezionale: edit sorgente aggiorna preview, edit valori aggiorna preview senza ricaricare.

**Lavoro stimato**: 2 PR, 3-4 giorni.

### M6. Markdown import/export — Fase 2 sub-step 📋

**Stato attuale**: solo JSON con schema v1. Markdown export con front-matter già esiste in v0.7.0 (per singolo prompt).

**Sotto-deliverable**:
- [ ] Markdown import da file singolo `.md` (parsing front-matter + body).
- [ ] Markdown import bulk da cartella `.md/`.
- [ ] Markdown export bulk (intero vault o cartella) come zip.
- [ ] Doc utente: pattern Obsidian/Foam compatibility.

**Lavoro stimato**: 2 PR, 3 giorni.

### M7. Coverage gap chiusi 🔧

**Stato attuale**: Rust client 74.14% / TS client non gated / Server 56% / MCP placeholder.

**Sotto-deliverable**:
- [ ] Rust client 74.14% → ≥ 80% line. Roadmap incrementale in `docs/operativo/coverage.md` (priorità `embeddings.rs`, `vault.rs`, `audit.rs`, `import_export.rs`). Gate CI alzato da 70% a 80%.
- [ ] TS client: configurare `vitest --coverage` + soglia 70% su `lib/*.ts`. Gate CI.
- [ ] MCP server: almeno 5 unit test su `sanitizzaFts`, `compila`, `estraiSegnaposti`, `parsing import`, `linting hook`. Gate CI > 0.
- [ ] Server Go: lasciare ferma a 56% (server è "v2.0 stuff", non blocca v1.0).

**Lavoro stimato**: 4-5 PR (uno per modulo target), 5-7 giorni.

### M8. Documentazione utente completa 📋

**Sotto-deliverable**:
- [ ] `docs/utente/getting-started.md` — primo setup, primo prompt, prima compilazione.
- [ ] `docs/utente/casi-uso/` — 5-10 ricette concrete (prompt per email, ricerca, summarize, code review, etc.).
- [ ] `docs/utente/troubleshooting.md` — FAQ + soluzioni a problemi noti.
- [ ] `docs/utente/glossario-sintassi.md` — `{{segnaposto}}`, `{{globale ...}}`, `{{import ...}}`, linting codes (LEN/PH/PII/STY/IMP).
- [ ] `docs/utente/scorciatoie-tastiera.md` — tabella completa hotkey.
- [ ] Rilettura/refresh di tutta `docs/utente/` esistente.

**Lavoro stimato**: 1 PR sostanziosa + revisioni, 5-7 giorni.

---

## Nice-to-have v1.0 (decisione caso per caso)

Item in `rinvii.md` che valuteremo se aggiungere al treno v1.0 in base a costo/beneficio. Non bloccano la release.

- **Statistiche distribuzione per cartella**: top importati copre l'80%, non urgente.
- **CLI `pap test <promptId> --golden=...`**: utile per CI/CD, ma forse meglio in v1.1.
- **Workflow path filter CI cleanup** (cli/mcp/server build YAML): cosmetico, 1 ora di lavoro.

---

## Out of scope v1.0 (vanno in v2.0 / Fase 5)

Riassunto qui per chiarezza, completo in [`fase-5-enterprise.md`](./fase-5-enterprise.md):

- Approval workflow + RBAC cartelle (Step 6+7 Fase 4 originali, riposizionati a Fase 5)
- MCP HTTP/SSE + `pap_create_draft` tool
- CLI comandi `login`/`new`/`import`/`export` (richiedono server API)
- Server Go cross-OS senza Docker (Step 0a Fase 5)
- SSO/OIDC/SAML
- E2E encryption
- Web app / Browser extension
- Webhook system
- Sync Git per prompt CI/CD
- Multi-utente (commenti, autore distribuzione statistiche, fork "pull request")
- Linting PII block-by-default (richiede E2E)
- Server endpoint `/sync/folders`, `?target=` filter

---

## Quality gate v1.0

- [ ] **Auto-update**: dimostrazione end-to-end con downgrade refuse + signature mismatch refuse su Win10 + Win11.
- [ ] **Coverage**: Rust client ≥ 80%, TS client ≥ 70%, MCP server > 0 test.
- [ ] **A11y**: `svelte-check` 0 warning a11y. CI gate `--fail-on-warnings` su a11y.
- [ ] **Security**: `security-audit.yml` verde, `cargo audit` verde, `govulncheck` (CLI) verde, `pnpm audit` verde.
- [ ] **Build matrix**: artifact Win + Linux + macOS verde su release pipeline.
- [ ] **Documentazione utente**: tutte le voci §M8 chiuse, link cross-check.
- [ ] **Smoke test manuale**: golden path utente nuovo su Win11 portable.
- [ ] **CHANGELOG**: pulito e completo da v0.8.8 → v1.0.0.
- [ ] **Tag GitHub**: release v1.0.0 promossa a Latest published, NSIS + portable + (eventualmente MSI) come asset principali.

---

## Strategia di branching v1.0

Sequenziale macro, parallelo micro:

- **Tutto su `main`**, niente branch lungo `feat/v1.0`.
- Una PR per ogni sub-deliverable, mergiata appena CI verde.
- Versioning durante lo sprint: continua patch line `v0.9.x` (es. `v0.9.0` "polish + auto-update", `v0.9.1` "import scopati", etc.). Il tag `v1.0.0` arriva solo quando il quality gate è ✅ chiuso.
- Bump a `v1.0.0` è un PR dedicato (`chore(release): bump v1.0.0`) con CHANGELOG narrativo.

### Parallelizzazione agenti consentita

Agenti possono lavorare in parallelo su fronti disgiunti:

| Agente | Layer | File principali |
|---|---|---|
| A — UI polish | Frontend Svelte | `apps/client/src/lib/superfici/*`, `*/components/*` |
| B — Auto-update | Packaging + signing | `.github/workflows/release.yml`, `tauri.conf.json`, NSIS template |
| C — Docs | `docs/utente/` | Mai tocca codice |
| D — Coverage | Test backend Rust + TS | `apps/client/src-tauri/src/*.rs` (test moduli), `apps/client/src/lib/*.test.ts` |

Conflitti attesi sui file condivisi (`lib.rs` invoke_handler, `ImpostazioniModal.svelte`, migration numbering) sono risolvibili con rebase incrementale dato che le PR sono piccole.

### Parallelizzazione agenti NON consentita

- Agente "v2.0 Enterprise" che scrive codice di produzione **PRIMA** di v1.0 chiusa. Resta solo design/spike (vedi §"Stream parallelo v2.0" sotto).

---

## Stream parallelo v2.0 — design only

Mentre v1.0 procede, un agente B può preparare strategicamente v2.0 producendo **solo artefatti non-codice di produzione**:

**Permesso**:
- Design doc in `docs/roadmap/v2.0-enterprise/*.md`
- Spike tecnici in `spikes/v2-*/` (codice esplorativo, mai mergato in `main` come feature)
- Valutazione tecnologica (es. confronto `modernc.org/sqlite` vs `mattn/go-sqlite3` per Step 0a)
- Architecture decision records in `docs/architettura/decisioni/v2-*.md`
- Mockup UI per nuove sezioni (SSO settings, E2E settings, etc.)

**Vietato**:
- Modifiche a `apps/client/`, `apps/server/`, `apps/mcp-server/`, `apps/cli/` in `main`
- Modifica a `package.json` / `Cargo.toml` per aggiungere deps "anticipate"
- Migration SQL nuove
- Nuovi cmd Tauri

Razionale: ogni byte aggiunto a `main` per v2.0 oggi è **sovra-ingegnerizzazione preventiva** finché non c'è cliente reale. Lo dichiara `fase-5-enterprise.md:19`.

---

## Riepilogo timeline indicativa

**Aggiornamento 2026-05-15**: cert Certum SimplySign Cloud arrivato. M1 può partire in parallelo a M2/M3 (era il blocker principale della timeline). Stima ricalibrata sotto.

| Mese | Focus | Tag rilascio |
|---|---|---|
| Mese 1 | **M1 auto-update (parallelo)** + M2 a11y + M3 sub-step Fase 4 + M6 Markdown | `v0.9.0`, `v0.9.1`, `v0.2.2` (NSIS+signing) |
| Mese 2 | Completamento M1 + M7 coverage + M4 import scopati | `v0.2.3` (Tauri Updater + latest.json) + `v0.9.2`, `v0.9.3` |
| Mese 3 | M5 doppia vista + M8 docs | `v0.9.4` |
| Fine mese 3 | Quality gate + bump v1.0.0 | `v1.0.0` GA |

**Stima realistica**: ~3 mesi totali dal 2026-05-15. Lo sblocco anticipato del cert recupera ~1 mese rispetto alla pianificazione precedente.

---

## Manutenzione di questo documento

- Quando un must-have atterra: spuntare la checkbox + linkare PR.
- Quando emerge nuovo item must-have: aggiungere come `M{n+1}` con razionale.
- Quando un nice-to-have viene promosso a must-have o demosso a v1.x: aggiornare entrambe le sezioni.
- Quando arriva il cert Certum: muovere M1 da 🔒 a 🚧 e iniziare il branch.
