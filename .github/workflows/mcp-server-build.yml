name: MCP Server Build

on:
  push:
    branches: [main]
    paths:
      - "apps/mcp-server/**"
      - "packages/shared-schema/**"
      - "pnpm-workspace.yaml"
      - "pnpm-lock.yaml"
      - "!apps/mcp-server/**/*.md"
      - "!apps/mcp-server/**/CHANGELOG.md"
      - "!apps/mcp-server/**/LICENSE"
      - "!packages/shared-schema/**/*.md"
      - "!packages/shared-schema/**/LICENSE"
  pull_request:
    branches: [main]
    paths:
      - "apps/mcp-server/**"
      - "packages/shared-schema/**"
      - "pnpm-workspace.yaml"
      - "pnpm-lock.yaml"
      - "!apps/mcp-server/**/*.md"
      - "!apps/mcp-server/**/CHANGELOG.md"
      - "!apps/mcp-server/**/LICENSE"
      - "!packages/shared-schema/**/*.md"
      - "!packages/shared-schema/**/LICENSE"
  workflow_dispatch:

jobs:
  lint-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: "22"
          cache: "pnpm"

      - name: Installa dipendenze
        run: pnpm install --frozen-lockfile

      - name: Type-check @pap/shared-schema (lint)
        run: pnpm --filter @pap/shared-schema lint

      - name: Test @pap/shared-schema con coverage (gate 80%)
        run: pnpm --filter @pap/shared-schema test:coverage

      - name: Build @pap/shared-schema
        run: pnpm --filter @pap/shared-schema build

      - name: Type-check @pap/mcp-server (lint)
        run: pnpm --filter @pap/mcp-server lint

      - name: Test @pap/mcp-server con coverage (gate 80%)
        run: pnpm --filter @pap/mcp-server test:coverage

      - name: Build @pap/mcp-server
        run: pnpm --filter @pap/mcp-server build

      - name: Verifica output
        run: |
          test -f apps/mcp-server/dist/index.js
          echo "Build artifact OK"
