{
  "name": "@pap/shared-schema",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Prompt a Porter — schema Zod e tipi TypeScript condivisi tra i client (MCP server, CLI, ecc.)",
  "license": "AGPL-3.0-only",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "zod": "^4.4.2"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "3.2.4",
    "typescript": "^6.0.3",
    "vitest": "3.2.4"
  }
}
