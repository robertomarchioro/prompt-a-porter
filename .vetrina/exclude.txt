# Esclusioni dalla vetrina pubblica.
# Una voce per riga: prefisso di directory (con / finale) o path file esatto.
# Confrontate contro l'output di `git ls-files` da scripts/publish-vetrina.sh:
# un file è escluso se uguale a una voce o se inizia con una voce-directory.
# Righe vuote e commenti (#) ignorati.

# --- Config/tooling interni ---
.claude/
.vetrina/
scripts/publish-vetrina.sh
.github/workflows/claude-code-review.yml

# --- Pianificazione e materiale interno (non per il pubblico) ---
docs/roadmap/
docs/test/
docs/operativo/
docs/architettura/
spikes/
