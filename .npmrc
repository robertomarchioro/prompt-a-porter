# Force public-hoist di Vue / @vue/* per VitePress (apps/site).
# VitePress 1.6 importa `vue/server-renderer` direttamente dai file
# generati .md.js: senza hoist, pnpm crea symlink non risolvibili
# da Rollup nel monorepo.
public-hoist-pattern[]=vue
public-hoist-pattern[]=@vue/*
